export interface IBenefitsTable {
    periodo: string;
    status: string;
    OrderEnrollment: string;
    OrderSustenance: string;
    TotalOrder: string;
}

export interface IBenefitsFilters {
    modalityID: number;
    foundId: number;
}